.. _vault-read:

Reading From a Vault
------------------------

The VaultReader Stream
=========================

As with the :ref:`VaultWriter <vault-write>`, the VaultReader is configured to act as a map of Stream objects, each associated with a 'file' inside the Vault. The interface for reading is considerably simpler, however

.. dropdown:: The VaultReader::Stream
	:color: primary
	:icon: search
	 
	.. jsl-class:: JSL::IO::VaultReader::Stream

The VaultReader Class
=========================

.. jsl-class:: JSL::IO::VaultReader
	:file: IO/Vault/VaultWriter.h
	:namespace: JSL::IO
	